/**
 * settings.spec.js — DISABLED
 *
 * Settings navigation on macOS desktop requires the native macOS menu bar,
 * which Playwright cannot interact with. Scoping this out until a viable
 * approach is found (e.g. IPC-based navigation or a dedicated settings route).
 */
